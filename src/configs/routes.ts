const PREFIX = '/';

export default {
  home: { path: `${PREFIX}` },
  bank: { path: `${PREFIX}/bank` },
  add: { path: `${PREFIX}/add` },
  ticket: { path: `${PREFIX}/events` },
  government: { path: `${PREFIX}/government` },
};
